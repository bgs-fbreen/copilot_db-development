# Database Configuration
DB_HOST=192.168.30.180
DB_PORT=5432
DB_NAME=copilot_db
DB_USER=frank
DB_PASSWORD=your_password_here

# Old Database (for migration)
OLD_DB_HOST=192.168.30.180
OLD_DB_PORT=5432
OLD_DB_NAME=db_accounts_old
OLD_DB_USER=frank
OLD_DB_PASSWORD=your_password_here

# OpenAI Configuration (for AI advisor features)
OPENAI_API_KEY=your-api-key-here

# Optional: Customize AI behavior (defaults shown)
# OPENAI_MODEL=gpt-4                    # Use gpt-3.5-turbo for lower costs
# OPENAI_TEMPERATURE=0.7                # 0.0-1.0, lower = more deterministic
# OPENAI_MAX_TOKENS=1500                # Max response length
